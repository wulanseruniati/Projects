namespace OthelloLogic
{
    // the grid is populated with states
    public enum BoardState
    {
        PLAYABLE_BLACK = 0,
        PLAYABLE_WHITE = 1,
        HIDDEN = 2,
        PLACED_BLACK = 3,
        PLACED_WHITE = 4
    }
}
